package com.example.gps_g11.Data.Budget;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class EnvelopeTest {
/*
    @Test
    void getFinalidade() {
        Envelope envelope = new Envelope("Compras",100.0);
        assertEquals("Compras",envelope.getFinalidade());
    }

    @Test
    void getValor() {

        Envelope envelope = new Envelope("Compras",100.0);
        assertEquals(100.0,envelope.getValor(),0.001);
    }

    @Test
    void getId() {
        Envelope envelope1 = new Envelope("Compras", 100.0);
        Envelope envelope2 = new Envelope("Educação", 200.0);
        assertEquals(1, envelope1.getId());
        assertEquals(2, envelope2.getId());
        assertNotEquals(envelope1.getId(), envelope2.getId());
    }*/
}