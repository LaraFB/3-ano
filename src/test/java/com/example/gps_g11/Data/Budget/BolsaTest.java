/*
package com.example.gps_g11.Data.Budget;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class BolsaTest {

    @Test
    public void testConstrutor() {
        Bolsa bolsa = new Bolsa("Minha Bolsa", 100.0, "Bolsa de Teste");
        assertEquals("Minha Bolsa", bolsa.getNome());
        assertEquals(100.0, bolsa.getValorReal(), 0.01);
        assertEquals("Bolsa de Teste", bolsa.getDescricao());
        assertEquals(100.0, bolsa.getValorDisponivel(), 0.01);
    }

    @Test
    public void testSettersAndGetters() {
        Bolsa bolsa = new Bolsa("Minha Bolsa", 100.0, "Bolsa de Teste");

        bolsa.setNome("Nova Bolsa");
        assertEquals("Nova Bolsa", bolsa.getNome());

        bolsa.setDescricao("Nova Descrição");
        assertEquals("Nova Descrição", bolsa.getDescricao());

        bolsa.setValorReal(200.0);
        assertEquals(200.0, bolsa.getValorReal(), 0.01);

        bolsa.setValorDisponivel(150.0);
        assertEquals(150.0, bolsa.getValorDisponivel(), 0.01);

        bolsa.setTotalDespesas(50.0);
        assertEquals(50.0, bolsa.getTotalDespesas(), 0.01);

        bolsa.setValorGasto(30.0);
        assertEquals(30.0, bolsa.getValorGasto(), 0.01);
    }
}
*/
